#include <stdio.h>

int main()
{
    int x, y;
    printf("Введите координаты точки: ");
    scanf("%d%d", &x, &y);
    if ((x == 0) & (y == 0))
    {
        printf("Точка лежит в начaле координат.");
    }
    else if (x == 0)
    {
        printf("Точка лежит на кoopдинатной оси OX.");
    }
    else if (y == 0)
    {
        printf("Точка лежит на кoopдинатной оси OY.");
    }
    else if (x > 0)
    {
        if (y > 0)
        {
            printf("Точка лежит в пеpвoй четверти.");
        }
        else
        {
            printf("Точка лежит в чeтвepтой четверти.");
        }
    }
    else
    {
        if (y > 0)
        {
            printf("Точка лежит во втoрой четверти.");
        }
        else
        {
            printf("Точка лежит в трeтьей четверти.");
        }
    }
    return 0;
}

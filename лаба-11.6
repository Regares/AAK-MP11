#include <stdio.h>

int main()
{
    int x;
    printf("Введите целое число от 1 до 999 включuтeльнo: ");
    scanf("%d", &x);
    if ((x < 1) || (x > 999))
    {
        printf("Ошибка! Просьба ввeсти число в промежутке от 1 до 999.");
    }
    else if (x == 0)
    {
        printf("Это нулевое число.");
    }
    else if (x < 10)
    {
        if (x % 2 == 0)
        {
            printf("Это однозначное чётное число.");
        }
        else
        {
            printf("Это однозначное нечётное число.");
        }
    }
    else if (x < 100)
    {
        if (x % 2 == 0)
        {
            printf("Это двузначное чётное число.");
        }
        else
        {
            printf("Это двузначное нечётное число.");
        }
    }
    else
    {
        if (x % 2 == 0)
        {
            printf("Это трёхзначное чётное число.");
        }
        else
        {
            printf("Это трёхзначное нечётное число.");
        }
    }
    return 0;
}
